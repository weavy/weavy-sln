@model App
@{
    var htmlAttributes = new Dictionary<string, object>() { { "autocomplete", "off" }, { "novalidate", "" } };
    if (ViewData["turboform"] != null) {
        htmlAttributes.Add("data-turboform", "");
    }
}

@using (Html.BeginForm(nameof(AppController.Insert), typeof(AppController).ControllerName(), new RouteValueDictionary(new { id = WeavyContext.Space.Id }), FormMethod.Post, htmlAttributes)) {
    @Html.Hidden("guid", Model.AppGuid);
    @Html.EditorsForModel()

    <div class="form-buttons">
        <button type="submit" class="btn btn-primary" name="operation" value="update">@T["Create"]</button>
        <a class="btn btn-outline-secondary" href="@Back" data-dismiss="modal">@T["Cancel"]</a>
</div>
}
