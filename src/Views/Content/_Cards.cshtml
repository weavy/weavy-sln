@model SearchResult<Content, ContentQuery>

@if (Model.Any()) {
    foreach (var item in Model) {
        Html.RenderPartialContent(Weavy.Core.Models.Layout.Card, item);
    }
    @Html.ScrollNext(Model)
}
