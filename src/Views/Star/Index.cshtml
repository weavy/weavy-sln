@{
    Layout = "~/Views/Shared/_PersonalLayout.cshtml";
}

<header class="navbar navbar-light sticky-top">
    <h6 class="navbar-title mr-auto px-1">Stars</h6>
</header>

<div id="tab-stars">
    <div class="tab-loading d-none">@Html.SvgSpinner()</div>
    <div class="tab-remote loaded" data-url="@(Url.Action<StarController>(c => c.Index()))"> @{ Html.RenderPartial("_Index"); }</div>
</div>
